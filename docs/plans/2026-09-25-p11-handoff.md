# Package P11 (Board and Menubar UI) Handoff

**Date:** 2026-09-25  
**Parent Branch:** `claude/happy-ptolemy-fke4jx` (PR #20, local only, no push to origin)  
**Active Worktree:** `/Users/alexandertar/GitHub/agent-swarm--p11` on branch `pkg/p11`  
**Base Commit:** `9988e61` (merge commit of Package P10)  

---

## 1. Operating Rules & Directives

1. **Single Coder Per Package & Package-Level Review:**
   - All units within a work package ("P") must be addressed sequentially by the **same** coder agent and reviewed together at the package boundary.
   - Units are structural guidance for the coder's TDD cycles (failing test → red → minimal implementation → green → commit per unit), **not** separate sub-tasks for different workers.
   - Do **not** dispatch separate coder agents for individual units within a package. A single coder agent implements Units 11.3, 11.4, and 11.5 sequentially before triggering the review loop.
2. **Subagent Policy:**
   - At most 1 active subagent at a time.
   - Model selection: Flash (high) for coder/implementer; Pro (low) for reviewer.
3. **Strict TDD:**
   - Every unit must strictly follow: failing test -> watch fail -> minimal implementation -> watch pass -> commit.
   - One commit per unit (conventional format, e.g. `feat(web): ... (unit 11.3)`).
4. **Local Only:**
   - Keep all branches and commits strictly local on `claude/happy-ptolemy-fke4jx` (no git push to remote origin).

---

## 2. Current State of Package P11

- **Unit 11.1: Designer and chore labels** — **COMPLETED** (`11fca8b`)
  - Web: Added `chore` to `ItemType`, `designer` to `Role`, `ITEM_TYPE_LABEL` alias, `ROLE_LABEL.designer = "Designer"`, `Wrench` icon, default designer in fixtures.
  - Menubar: Added `.designer` to `Role` and `SettingsRole`, `Settings.defaults` designer row, `Copy.roleLabel` and `Copy.defaultsRowLabel`, `.designer` in `SettingsModel.defaultsOrder`, updated fixture files.
  - Tests: `copy.test.ts` (web), `testRoleDecodesDesigner` and `testDefaultsOrderIncludesDesigner` (Swift) all passing.

- **Unit 11.2: HTTP payload fields** — **COMPLETED** (`47898f1`)
  - Go runtime: Implemented `StepForAgent(ctx, agentID)` in `internal/runtime/workflow.go`.
  - HTTP API: In `internal/httpapi/items.go` (`s.getItem`), populated `workflow_state` and `crew` from `s.RT.WorkflowFor(ctx, key)` for workflow tasks (omitted for legacy tasks).
  - HTTP API: In `internal/httpapi/runtime.go`, added `Step *string` to `agentNodeWire`, populated from `StepForAgent`. In `internal/mcpserver/tools.go`, added `step` to `s.agentOut`.
  - Tests: `TestItemDetailIncludesWorkflowState` and `TestAgentsPayloadIncludesStep` passing. All Go tests pass.

---

## 3. Remaining Work for Next Agent

Dispatch a **single Flash coder subagent** to execute Units 11.3, 11.4, and 11.5 sequentially.

### Unit 11.3: `WorkflowSection` in Details Panel
- **Files:** `web/src/components/WorkflowSection.tsx`, `web/src/panels/Details.tsx`, `web/src/types.ts`
- **Tests:** `web/src/components/WorkflowSection.test.tsx` and additions to `web/src/panels/Details.test.tsx`
- **Specification (Spec B9, Screens, Copy):**
  - **Header:** `"Workflow · {template} · {state} · Round {round} of {max_rounds}"` (e.g. `Workflow · tdd-reviewed · Running · Round 2 of 3`).
  - **Step / Run rows:** One row per step (`build`, `review`), sub-row per run:
    - Role emoji + role label (`Copy.roleLabel` / `ROLE_LABEL`)
    - Agent name (link: open terminal)
    - State chip
    - Verdict chip: `Pass` (green), `Changes requested` (amber), `Blocked` (red)
    - Findings disclosure: `"{n} findings"` expanding to list `[major] internal/x.go:42 — summary` (displaying `[unit <n>]` tag if finding has `unit`).
  - **Escalation banner:** When workflow state is `"escalated"`, display red banner with escalation reason and hint `"The orchestrator decides next."`.
  - **Hidden state:** Hidden when item has no workflow (`workflow_state` absent or `item.workflow == null`).
- **TDD:** Write failing tests in `WorkflowSection.test.tsx` and `Details.test.tsx`, verify RED, implement, verify GREEN, commit: `feat(web): workflow section in details panel (unit 11.3)`.

### Unit 11.4: Kanban Crew and Plan Warnings
- **Files:** `web/src/views/Kanban.tsx`, `web/src/logic/kanban.ts`, `web/src/views/Reviews.tsx` (or Needs you / Plan approval review)
- **Tests:** `web/src/views/Kanban.test.tsx`, `web/src/logic/kanban.test.ts`, plan review tests
- **Specification (Spec B9, Screens, Copy):**
  - **Kanban task card:**
    - Crew row: small role emoji per active agent (up to 3 emojis, `+n` when more than 3 active agents).
    - Round badge: `"R{n}"` badge when `round > 1` (e.g. `R2`).
  - **Plan review screen (Needs you → plan approval):**
    - Render warnings from C2 plan validation listed above the section diff under header `"Plan warnings"`.
- **TDD:** Write failing tests, verify RED, implement, verify GREEN, commit: `feat(web): kanban crew row, round badge and plan warnings (unit 11.4)`.

### Unit 11.5: Menubar Step Suffix
- **Files:** `apps/menubar/Sources/SwarmBarKit/Wire.swift`, `AgentActions.swift`, `Copy.swift`
- **Tests:** `apps/menubar/Tests/SwarmBarKitTests/` (specifically `testAgentRowStepSuffix`)
- **Specification (Spec B9, Screens, Copy):**
  - In `Wire.swift`, add `public var step: String?` to `AgentNode`.
  - In `AgentActions.swift`, update agent subtitle / row formatting: when `a.step` is present, append step id suffix:
    - e.g. `"Reviewer · review r2"` or `"Reviewer · review"` or `"Coder · build"`.
- **TDD:** Write failing test `testAgentRowStepSuffix`, verify RED, implement, verify GREEN, commit: `feat(menubar): agent row step suffix (unit 11.5)`.

---

## 4. Package Review, Verification & Merge (Package Boundary)

Once Units 11.3, 11.4, and 11.5 are committed by the coder agent:

1. **Run full verification:**
   ```bash
   go test ./internal/httpapi/...
   cd web && pnpm typecheck && pnpm test
   cd apps/menubar && swift test
   ```
2. **Review:**
   - Dispatch Pro code reviewer subagent against Package P11 acceptance criteria and diff.
   - Address any findings with fix commits.
3. **Merge & Prune:**
   - Merge `pkg/p11` into base branch `claude/happy-ptolemy-fke4jx` with `--no-ff`.
   - Prune worktree `/Users/alexandertar/GitHub/agent-swarm--p11` using `git worktree remove`.
   - Proceed to Package P12 (`Planning emits role-assigned packages`).
