# P3 fix round 2 (Opus re-review: all 8 round-1 findings ADDRESSED)

1. (Important, new) skills/swarm-coder/SKILL.md:18 vs :34: step 2 says only "record {phase: green, ok: true, unit: <n>}" without saying to put it in a checkpoint, while :34 says per-unit red/green entries "were already recorded as progress checkpoints (step 1 and 2)" and completed "doesn't re-create or backfill them". The tdd gate (spec B5 ~672-677) needs a green entry per unit in this attempt's checkpoints; Verify entries in completed have no phase/unit. Fix: step 2 → "record it in a `progress` checkpoint" (or explicitly allow the green entries in the next checkpoint, completed included). Pin it in the test row.
2. (Minor) skills/swarm/SKILL.md:13 and :27 give the reverse reason of spec B4 (~656-659): the daemon does NOT relay accepted/progress/completed for workflow agents; the engine sends one workflow_succeeded/workflow_escalated. Reword: "the daemon reports the workflow's outcome to your orchestrator". Add `handoff` to the list of things that still reach the orchestrator (B4 says it does).
3. (Check and state) Does an assignment_update fix round start a new attempt (so the tdd gate needs a fresh red→green pair per unit touched)? Read spec B4/B5/B6 and say it explicitly in swarm-coder's "Fix rounds" section, matching the spec. If the spec doesn't decide it, report that instead of inventing a rule.
