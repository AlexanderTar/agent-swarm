# PA fix round 1 (Opus review: D1-D5 ✅, probe sound, 08:33 real-home agy run was NOT this session)
1. (Important) internal/install/agy.go:234 via copyTree (plugins.go:394): a symlinked entry in the legacy dir aborts all of `swarm install` for agy ("read …/skills/linked: is a directory"), so hooks/MCP/WriteSkills never run. Fix: in the loop, if e.Type()&os.ModeSymlink != 0, recreate the link (Readlink + Symlink) at the destination; test (red first) with a legacy symlinked entry.
2. (Minor) Say in the report, spec A7 D3 and the plan that the repair also runs from `swarm migrate` step 9 (cmd/swarm/migrate.go:72-86) — an explicit user command.
3. (Minor) agy.go:254 doctor text: a dangling old link (reaped session) or a missing old path must not print "…is not inside a session folder"; report each case accurately (dangling link into run/launch → the same warn as decision 4; missing → OK, nothing to say). Test the dangling case.
4. (Minor) agy.go:169-195 legacy detection: check the FIRST-hop Readlink target against <Home>/run/launch (catches a dangling first hop and a first hop into run/launch that resolves back to config/skills); simplify using EvalSymlinks(oldPath) and reuse the under-dir helper from skills.go:234-235 instead of duplicating it. Tests for both new shapes.
5. (Minor) agy_test.go:314: add a legacy-only entry and assert it is salvaged into the new root; extend TestWriteAgyLeavesAHealthySkillsRootAlone for old path = real dir and = link to the new root.
6. One more sealed probe run (same safety rules as before; before/after real-home snapshot diff): make $HOME/.gemini/config/skills ITSELF a symlink to a scratch dir (the production shape), with the probe skill inside, and confirm agy discovers it. Record in docs/plans/2026-09-24-skill-symlink-probe.md. If it fails, set agy back to Copy and say so.
7. (Minor) Report/probe doc: note the transient over-copy of agy state into the scratch HOME (deleted before any run) and the unsealed `agy --version`; add skills.go/skills_test.go to the plan's PA Files list; state the accepted trade-offs (setupEnv MkdirAll of the real ~/.gemini/config/skills per spawn; resumed legacy agy-homes keep a stale private copy) in spec A7.
New commits; append "Fix round 1" to the report; reply with the short status contract.
