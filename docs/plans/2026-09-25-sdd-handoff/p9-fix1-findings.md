# P9 fix round 1 (Opus review) + user directive. Legacy guarantee verified ✅ — keep it.

USER DIRECTIVE (binding, overrides your current fallback): a fix round must NEVER spawn a fresh builder while the old builder's session is still live. The daemon coordinates: close the old session first (end its tmux pane/session the way the daemon already ends completed sessions — find the existing kill path, e.g. what killCompletedAfter uses, and call it synchronously), wait until it is terminal, then Retry(builder, note) on the same agent. Remove the "fall back to fresh spawn" path entirely. A fresh spawn of the fix step is only legitimate when the builder agent itself no longer exists / can't be retried for a reason other than "still live" — and then findings must still reach it (I4). Test: RetryFix while the builder pane is still live → old session terminated first, same agent retried, never two builder sessions at once.

Important:
I1. Runs stranded `active` forever (workflow.go:1065, :891→904, Cancel agents.go:1306): startup failure via watchStartup→failSession, a direct session cancel, or a crash between the applyAutoRetry claim and Retry leave the run active; Next waits; recoverWorkflows skips workflows with an active run. Fix: in the stall scan / before Next, treat an active run whose agent's latest session isn't live as failed (or cancelled when the session is cancelled); change the recovery exclusion to "has an active run with a live session". Tests for each of the three paths.
I2. Spawn before the row is claimed (workflow.go:669→686, applyRetryFix:739→753): Share error / crash / ctx cancel between Spawn and writing agent_id leaves the row waiting with a live agent → the stall scan spawns a second builder. Spawn that returns an error after committing the agent row (agents.go:922) makes every stall scan create a new agent every 30 s forever. Fix: record agent_id right after Spawn returns, before Share; log (don't fail) Share errors; count spawn failures toward auto_retries so they escalate; validate every worktree entry in StartWorkflow; run post-commit advances under context.WithoutCancel(ctx). Tests: Share failure doesn't double-spawn; repeated spawn failure escalates.
I3. Review worktree only created on first insert (workflow.go:466): on replay after a crash newRoles is empty → reviewers spawn with no worktree. Fix: create/attach when rows for (step, round) have NULL review_worktree_id and action.SHA != "". Test.
I4. Findings and the resume note only reach an existing agent (applyRetryFix:739-752, spawnRunAgent:647, ResumeWorkflow:1238-1250). Resume after blocked/stale yields Spawn{build, Round:2, Findings:[]}. Fix: in spawnRunAgent when round > 1, render the round-1 changes_requested/blocked findings of the review step targeting this step into the brief (reuse the R3 lookup P8 wrote in checkpoint.go — findFixStepsFor/fixRoundFindings — don't write a third copy); persist the resume note (e.g. workflows.context_json) and deliver it when the agent spawns. Extend TestResumeRetryAfterBlocked to assert findings reach the builder.
I5. resumeBumpsRound re-implements the planner and is wrong (workflow.go:1114; reviewer probe: scratchpad/p9probe/internal/runtime/zz_probe_test.go — ui_reviewer crash case and stale changes_requested case both re-escalate). Fix: delete it; use `if workflow.Next(spec, runs, wf.Round, wf.ExtraRounds+1).Kind == workflow.ActionEscalate { newRound++ }` (verify this against both probe cases; adjust if the probe shows otherwise and explain). Add both as tests.
I6. Pause treated as crash (reconcile.go:656): a timed-out pause ends `interrupted`, the run is marked failed, AutoRetry un-pauses the agent. Fix: leave the run active on the interrupted branch. Test.
I7. FIFO across workflows (workflow.go:1031): `GROUP BY w.id ORDER BY MIN(r.created_at)`; fillWaitingRuns yields when the owner has an older waiting run in another workflow. Test with two workflows.
I8. Resume/cancel: check it.RootID == orch.RootItemID (copy: reuse StartWorkflow's refusal copy shape; add to spec copy list if new); CancelWorkflow takes lockForWorkflow, flips state first, then cancels agents, marks runs cancelled; guard ResumeWorkflow UPDATE with AND state='escalated'. Tests: foreign orchestrator refused; cancel racing a slot release spawns nothing.
I9. deliverNote duplicates Retry's message INSERT (workflow.go:1176 vs agents.go:1508-1517): extract one helper.

Minor (fold in): remove toCloseFailedSelf if startSession already kills a.Name before Tmux.Start (verify agents.go:1095 first); fix the "claims its row first" comments; merge workflowSucceeded/workflowFailedOrCancelled into latestWorkflowState and dedupe latestWorkflowRow/workflowRowByID; resume accept/fail remove review worktrees; StartWorkflow: if advance fails after commit, don't return an error for the committed start (log + let recovery advance), and map the workflows_one_live constraint to the "already has a running workflow" copy; fix the misplaced TestCheckpointTriggersAdvance doc comment; add a concurrent-advance idempotency test (two goroutines advancing the same workflow → one spawn).
Do not implement P10's request_id idempotency here.

Verify: go test ./internal/runtime/... ./internal/items/... ./internal/hook/... ./internal/workflow/... -count=1 ; go build ./... && go vet ./... ; go test ./... once; go test -race ./internal/runtime/ -run Workflow once.
