# PM controller ruling (2026-09-25) — proceed to Phase 2 with the proposed design (bullets 1-11), with these decisions:
Q1. Drop ALL operator MCP servers from spawned muse; swarm-only (matches the user's request and codex).
Q2. Accept the shared real XDG_DATA_HOME (plugin-store integrity forces it; claude spawns likewise share their plugin cache). Pin XDG_DATA_HOME/XDG_STATE_HOME/XDG_CACHE_HOME to the real paths explicitly. Document the trade-off in spec A8.
Q3. First unit of Phase 2 = the live TUI probe: launch muse interactively in a scratch tmux session with exactly the proposed env (isolated HOME + XDG_CONFIG_HOME + pinned data dirs), capture the pane (tmux capture-pane) at 5 s / 15 s / 30 s, confirm no blocking first-run or foreign-context dialog, then exit it. If a dialog blocks, record its exact text and implement StartupDialogs() for it (pattern: other adapters' StartupDialogs). Record evidence in docs/plans/2026-09-25-muse-isolation-probe.md (commands, env, pane captures, verdict), plus the Phase 1 findings summary.
Also: the HOME denylist also excludes `.muse` if present and `.config` (handled by XDG). Keep `.ssh`, `.gitconfig`, toolchains etc. via the symlink-everything-else loop. Resume must use the same env.
Docs: spec section "A8. muse spawn isolation" (after A7 if present, else after A6) with the decisions + trade-off; "### PM: muse spawn isolation" package in docs/plans/2026-09-24-self-contained-tasks-and-role-skills.md; tick at the end.
Units (test-first, temp homes): PM.1 live TUI probe + doc; PM.2 HOME denylist isolation + pinned XDG data/state/cache; PM.3 settings clone-then-mutate (swarm-only mcpServers, foreign_personal_skills/rules false); PM.4 skills via install.LinkSkills (swarm skills only). Tests (a)-(f) from your report plus: Resume returns the same isolation env.
Verify: go test ./internal/adapter/... ./internal/install/... ; go build ./... && go vet ./... ; go test ./... once. Snapshot rule for the real muse dirs still applies (session/log files excepted).
