# P12 handoff — role-assigned planning packages

Branch: `pkg/p12` (base `ec93517`). One coder completed units 12.1–12.5 sequentially. Each unit has its own conventional commit; the package was verified after all five commits.

| Unit | RED witness | GREEN witness | Commit |
|---|---|---|---|
| 12.1 Tree fields/materialize | `go test ./internal/runtime -run 'Test(ParseTreeWithWorkflowUnitsSolo\|MaterializeCopiesWorkflowUnitsVerify\|MaterializeDerivesRoleHint)'` failed to compile: missing TreeNode fields/TreeUnit | Same test passed; `go test ./internal/runtime/...`, build, vet passed | `15746f6` |
| 12.2 Plan lint/warnings | `go test ./internal/runtime -run TestTree` failed: undefined `lintTree`; `TestPlanWarningsPersistPerRevision` failed: missing Warnings fields; per-unit warning test failed with `warnings=[]` | Runtime, MCP server and DB suites passed; GET artifact revision test passed; build and vet passed | `de814ea` |
| 12.3 Workflow skill | `TestWorkflowsSkillExampleValidates` failed: missing fenced example | Parsed, linted and validated the example with no errors or warnings; install checks, build and vet passed | `f8c974d` |
| 12.4 Spike skill | `TestSpikeSkillSevenStepMethod` failed on all seven stages and required references | Skill test, package-boundary contract test, build and vet passed | `5855ea9` |
| 12.5 Orchestrator/README | `TestOrchestratorSkillNoLongerHandRollsReviews` failed on workflow delivery, superpowers references and ponytail-debt | Skill and existing one-coder/package-boundary tests, build and vet passed | `c66be06` |

Plan registration now validates each workflow at its node level, rejects missing or mismatched task roles, review tasks, incomplete TDD scripts and invalid units, and returns batching/TDD warnings. Warnings are stored in `artifact_revisions.warnings_json` by revision, returned by `swarm_artifact`, and exposed as `warnings: string[]` by `GET /api/artifacts/{id}?revision={n}` for the P11 Review screen. Materialization copies resolved workflows, execution scripts, units, solo reasons and verify commands, and derives task role hints. Legacy tasks with NULL `workflow_json` retain their existing execution path.

The workflow skill's fenced example is exercised by a parser/lint/validation test. The spike and orchestrator skills describe research/design workflows, single-coder sequential package units, one package review, escalation/follow-up handling, story review and root integration. The README covers roles, skills, workflow delivery and vendored licenses. `make skills-sync` refreshed the install mirror after every skill edit.

Final verification:

- `make skills-sync` — passed.
- `go test ./internal/runtime/... ./internal/install/... ./internal/mcpserver/... ./internal/db/...` — passed.
- `cd web && pnpm test` — 49 files passed, 1 skipped; 425 tests passed, 10 skipped.
- `cd web && pnpm build` — passed, including typecheck.
- `go test ./internal/httpapi/...` — passed, including revision warning response.
- `go test ./...` — passed.
- `go build ./...`, `go vet ./...`, `git diff --check` — passed.

The first web test attempt could not run because this fresh worktree had no `node_modules`; `pnpm install --frozen-lockfile` restored dependencies and the rerun passed. The first full HTTP suite attempt needed built web assets; it passed after `pnpm build`. No outstanding test failures are known.
