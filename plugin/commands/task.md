# Task

Put the current work on the board. Use the swarm-task skill: `swarm_task_create` with a
title and summary you write from this conversation, or `swarm_task_update {key, summary}`
if it is already there, or `swarm_task_join {key}` to help on someone else's.
