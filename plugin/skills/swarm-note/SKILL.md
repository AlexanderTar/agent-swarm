---
name: swarm-note
description: Save a durable reference note to the Agent Swarm knowledge base for other agents to find later.
---

# Swarm Note

Use `swarm_kb_write` with subdir `notes`, a descriptive filename, and markdown body.
Include frontmatter fields: title, tags, task (if related), links.
