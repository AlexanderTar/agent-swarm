---
name: swarm-search
description: Search the system-wide semantic knowledge base under ~/.swarm/kb for decisions, notes, and handoffs.
---

# Swarm KB Search

Use MCP tool `swarm_kb_search` with a natural language query.
For exact references use `swarm_kb_get` with slug from frontmatter.
Write new notes with `swarm_kb_write` to `notes/` or `decisions/`.
